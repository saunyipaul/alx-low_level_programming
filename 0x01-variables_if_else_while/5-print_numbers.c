#include <stdio.h>

/**
 * main - Entry point
 *
 * Do while - to do iterations
 *
 * Return: Always (0) success
 *
 */

int main(void)
{
	int n = 0;

	while (n < 10)
	{
		printf("%d", n);
		n++;
	}
	printf("\n");
	return (0);
}
