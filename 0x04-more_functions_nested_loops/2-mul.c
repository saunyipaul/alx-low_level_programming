#include <stdio.h>
#include "main.h"

/**
 * mul - Wntry point of function
 * program to multiply two numbers
 * @a: variable one
 * @b: variable two
 * Return: always 0 success
 */

int mul(int a, int b)
{
	int ans;

	ans = a * b;
	printf("%d\n", ans);

	return (0);
}
